#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	char *street = "Rue des Marchands";
	char *city = "Toulouse";
	char state = 'F';

	_printf("%d, %s, %s %c %i\n", 37, street, city, state, 31000);

	return (0);
}
